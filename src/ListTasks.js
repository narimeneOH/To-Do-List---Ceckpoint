import React from 'react';

function ListTasks(){
    const tasks = ['task1', 'task2', 'task3'];
    const TaskList = tasks.map(task => this.state)
    return(
        <div>
            {TaskList}
        </div>
    )
}

export default ListTasks



